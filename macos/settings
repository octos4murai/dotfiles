# Settings for MacOS

# To use uBar instead of the Dock, do not use uBar's dockless mode.
# Instead hide the dock and set an impossibly long delay:
defaults write com.apple.Dock autohide-delay -float 1000 && killall Dock

# If using Vim keybindings on Mac, enable key repeat:
defaults write com.microsoft.VSCode ApplePressAndHoldEnabled -bool false